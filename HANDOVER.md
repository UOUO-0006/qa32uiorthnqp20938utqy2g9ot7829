# ARG Web開発・運用仕様書 (AI/開発者引き継ぎ用)

本ドキュメントは、本ARG（代替現実ゲーム）プロジェクトにおけるWebページ作成・更新時の全技術仕様および開発ルールです。
別のAIや開発者が作業を行う際は、必ず本仕様に従ってください。

---

## 1. プロジェクト情報

- **GitHub ユーザー**: `UOUO-0006`
- **リポジトリ名**: `qa32uiorthnqp20938utqy2g9ot7829`
- **デフォルトブランチ**: `main`
- **GitHub Pages URL**: `https://uouo-0006.github.io/qa32uiorthnqp20938utqy2g9ot7829/`

---

## 2. 厳格禁止事項 (ソースコード隠蔽)

開発者ツール（F12）によるソース閲覧対策のため、以下の「親切設計」は全ページで完全禁止。

1. **ソースコード内コメント (`<!-- ... -->`) の完全禁止**
2. **意味のあるクラス名・ID名・変数名・ファイル名の完全禁止**
   - **ファイル名ルール**: 
     - エントリページ：固定ファイル名 `index.html`（永久不変。ユーザーがブックマークするURLのため更新時も変更しない）
     - 中間ページ：固定ファイル名 `main.html`（永久不変）
     - 補助ページ：状態や内容に関係なく「ランダムな8桁の英数字」にすること。
   - クラス・ID・変数名: `c`, `i`, `l`, `p` などの無機質・短縮記号のみ使用。
3. **`alt` 属性テキストの禁止** (常に空文字 `alt=""`)
4. **意味のあるページタイトルの禁止**

---

## 3. 必須ブラウザ仕様回避策

### (1) タブ名完全空白保持 (`U+2800` 点字空白)

- Chrome等の `trim()` 削除によるURL自動補完表示を回避するため、点字空白 `&#10240;` (`U+2800`) を設定。
- HTML: `<title>&#10240;</title>`
- JS: `document.title = "\u2800";`

### (2) 検索エンジン除外 (`noindex`)

- 全ページの `<head>` に記述: `<meta name="robots" content="noindex, nofollow">`

---

## 4. デザイン・UI共通基本仕様

- **背景色**: 完全な黒 (`background-color: #000000;`)
- **Webフォント**: リポジトリ直下の `x12y16pxMaruMonica.ttf` を `@font-face` で全要素に適用。

```
@font-face {
    font-family: 'MaruMonica';
    src: url('x12y16pxMaruMonica.ttf') format('truetype');
}
* {
    font-family: 'MaruMonica', sans-serif;
}
```

- **画面配置**:
  - **上部要素/画像**: 画面の中央上部 (`margin-top: 20px; max-height: 45vh;`)
  - **下部操作要素/リンク**: 画面の中央より下 (`top: 65vh;` 〜 `68vh`)
  - **標準リンクスタイル**: `rgb(50, 117, 198)`、下線あり、サイズ `3rem`

### (1) 再生・選択・遷移UI

中間ページ（`main.html`）に配置するUI。

- **再生UI**: オーディオファイルをその場で再生する要素（ページ遷移なし、音量初期値50%）
- **画像選択UI**: 左右の矢印で画像を切り替える要素（画像はファイル名を表示しない、表示されるのは画像のみ）
- **リンク**: 画像選択状態に応じて異なるページに遷移する要素（Base64難読化）

配置: 横並び（再生UI → 画像選択UI → リンク）

### (2) 補助ページ

画像選択状態に応じて遷移するページ。

- **構造**: 上部に画像、下部に復帰リンク
- **復帰リンク**: エントリページまたは中間ページへ遷移（Base64難読化）

---

## 5. ファイル更新 & リンク難読化運用ルール

1. **ファイル構成**:
   - **エントリページは `index.html` で永久固定**（乱数なし・最簡形式。更新時も名前を変えない。ユーザーがブックマークするURLのため）
   - **中間ページは `main.html` で永久固定**（乱数なし・最簡形式。更新時も名前を変えない）
   - 補助ページ: 推測不可能な8桁ランダム英数字ファイル名。
   - `rotate.html` など、推測可能・意味のある補助ページ名は **完全禁止**。
2. **更新運用**:
   - エントリファイル `index.html` は名前を変えず内容のみ更新する。
   - 中間ファイル `main.html` は名前を変えず内容のみ更新する。
   - 補助ページを更新・改修するたびに、新しく生成したランダムな8桁英数字ファイルを作成し、旧ファイルは削除する。エントリ内のBase64リンクも新ファイル名に更新する。
3. **リンク難読化**:
   - 遷移先URLの直接閲覧防止のため、JavaScriptで Base64 エンコード等を行い動的に遷移させる。全ページの遷移リンク（戻りリンク含む）に適用する。

```
const p = "aW5kZXguaHRtbA=="; // "index.html" のBase64
document.getElementById("l").addEventListener("click", function() {
  window.location.href = atob(p);
});
```

---

# 6. ローカル開発・GitHub Pages公開時の必須互換性仕様

本プロジェクトでは、HTMLをローカル環境で作成・検証した後、GitHub Pagesへ配置して公開する。

したがって、 **「ローカルで動作すること」と「GitHub Pages上で動作すること」の両方を満たすことを完成条件とする。**

AI・開発者は、ローカル環境だけで動作確認して完成と判断してはならない。

## 6.1 パス指定

### 原則

HTML、CSS、JavaScript、画像、フォント等のファイル参照は、原則として **相対パス** を使用する。

例:

```
<img src="image.png" alt="">
```

```
src: url('x12y16pxMaruMonica.ttf') format('truetype');
```

```
window.location.href = "kPMniGhz.html";
```

### 禁止

PC上の絶対パスを使用してはならない。

```
C:\Users\...
C:/Users/...
file:///C:/...
```

これらはGitHub Pages上では存在しないため使用禁止。

### `/` から始まるパスについて

`/image.png`、`/css/style.css` 等の **ドメインルート相対パスは原則使用禁止**。

GitHub Pagesがリポジトリをサブパスで公開する構成の場合、

```
https://ユーザー名.github.io/リポジトリ名/
```

となるため、`/image.png` はGitHub Pagesのドメイン直下を参照してしまう可能性がある。

本プロジェクトでは、特別な理由がない限り以下を使用する。

```
image.png
./image.png
../image.png
```

## 6.2 ファイル名の大文字・小文字

GitHub Pagesの公開環境では、ファイル名の大文字・小文字を区別する。

したがって、参照先と実際のファイル名を **完全一致** させる。

例:

```
実際: Image.png
参照: image.png
```

これはローカルのWindows環境では動作する場合があるが、GitHub Pagesでは正常に読み込めない可能性がある。

AI・開発者はファイルを作成・変更した際、以下を必ず確認する。

- ファイル名
- 拡張子
- 大文字・小文字
- ディレクトリ名
- HTML/CSS/JS内の参照先

これらを完全一致させること。

## 6.3 ファイルの配置関係

HTMLから参照するファイルは、 **GitHubリポジトリに実際に存在する相対位置** を基準に指定する。

例えば、

```
index.html
main.html
image.png
x12y16pxMaruMonica.ttf
```

という構成なら、

```
<img src="image.png" alt="">
```

および

```
src: url('x12y16pxMaruMonica.ttf') format('truetype');
```

で参照する。

内部ページがサブディレクトリにある場合は、HTMLから見た相対位置を正確に指定する。

AIは「ローカルでファイルが見つかるか」ではなく、 **公開後のディレクトリ構造でも同じ相対関係になるか** を確認すること。

## 6.4 `index.html` の扱い

`index.html` はエントリページとして永久固定する。

- ファイル名を変更しない
- ランダム化しない
- 更新時も同じ `index.html` を使用する
- GitHub Pagesの公開ルートに配置する
- エントリURLとして外部に告知するURLは変更しない

## 6.5 `main.html` の扱い

`main.html` は中間ページとして永久固定する。

- ファイル名を変更しない
- ランダム化しない
- 更新時も同じ `main.html` を使用する
- GitHub Pagesの公開ルートに配置する

## 6.6 補助ページのランダムファイル名

補助ページは8桁のランダム英数字ファイル名を使用する。

補助ページを更新・改修する場合は、

1. 新しい8桁ランダム英数字ファイル名を生成する
2. 新ファイルを作成する
3. `main.html` および他の遷移元ページから新ファイルへ遷移するよう修正する
4. Base64化した遷移先も新ファイル名に更新する
5. 旧補助ページを削除する
6. 旧ファイルへの参照が残っていないことを確認する

という順序で処理する。

## 6.7 GitHub Pagesで使用できないローカル依存

ローカルPCにしか存在しないファイル、フォルダ、サービス等に依存してはならない。

例えば以下は禁止。

```
C:\Users\...
ローカル専用API
ローカル専用サーバー設定
PC内の別フォルダに存在する画像
PC内の別フォルダに存在するフォント
```

公開に必要なファイルは、原則としてGitHubリポジトリ内に含める。

## 6.8 `file://` と `https://` の挙動差

ローカルHTMLを直接ダブルクリックして、

```
file:///
```

として開いた場合と、GitHub Pages上の

```
https://
```

で開いた場合では、ブラウザのセキュリティ制約やリソース読み込み条件が異なる。

特に以下を使用する場合は注意する。

- `fetch()`
- ES Modules (`type="module"`)
- 外部リソース
- JavaScriptによるファイル読み込み
- JSON等のデータファイル読み込み

ローカルの `file://` で正常に動かないことだけを理由に実装を変更してはならない。

逆に、ローカルで正常でもGitHub Pagesで正常とは限らない。

必要に応じてローカルHTTPサーバーを使用し、

```
http://localhost:...
```

で確認する。

## 6.9 GitHub Pages固有の公開確認

GitHubへpushした後、GitHub Pages上の実際のURLで確認する。

確認対象:

- `index.html` が表示される
- 画像が表示される
- Webフォントが読み込まれる
- CSSが適用される
- JavaScriptが実行される
- 内部ページへ遷移できる
- 内部ページから戻れる
- Base64による遷移が正常に動作する
- 画像選択UIが正常に動作する
- 再生UIが正常に動作する
- 補助ページが正常に動作する
- 404が発生していない
- コンソールに重大なエラーがない

**GitHub上のファイル一覧を見て「ファイルが存在する」だけでは検証完了としない。**

実際の公開URLからページを開いて動作を確認すること。

---

# 7. AIによる作成・更新時の必須チェック

AIはHTMLを作成・変更する際、コードを出力する前に以下を内部的に確認する。

### ファイル構成

- [ ] `index.html` が存在する
- [ ] `index.html` の名前を変更していない
- [ ] `main.html` が存在する
- [ ] `main.html` の名前を変更していない
- [ ] 補助ページが8桁ランダム英数字になっている
- [ ] 補助ページに意味のあるファイル名を使用していない
- [ ] 必要な画像・フォント等がリポジトリ内に存在する

### パス

- [ ] Windowsの絶対パスがない
- [ ] `file:///` がない
- [ ] 不要なドメインルート相対パス `/...` がない
- [ ] 相対パスが実際のファイル配置と一致している
- [ ] ファイル名の大文字・小文字が完全一致している
- [ ] 拡張子が一致している
- [ ] `../` の階層数が正しい

### HTML

- [ ] ソースコードコメントがない
- [ ] `alt` がすべて空文字 `alt=""`
- [ ] `<title>&#10240;</title>` を使用している
- [ ] `noindex, nofollow` がある
- [ ] 意味のあるclass名・ID名を使用していない

### JavaScript

- [ ] 変数名が無機質・短縮記号になっている
- [ ] 内部ページへの遷移がBase64等で難読化されている
- [ ] 戻りリンクも難読化されている
- [ ] `document.title = "\u2800";` を維持している
- [ ] ローカルPC固有のパスを参照していない

### CSS

- [ ] 背景が完全な黒
- [ ] 指定フォントを使用している
- [ ] 必須レイアウトを維持している
- [ ] リンク色が `rgb(50, 117, 198)` になっている

### 再生・選択・遷移UI

- [ ] 再生UIがページ遷移なしで動作する
- [ ] 音量初期値が50%になっている
- [ ] 画像選択UIが左右の矢印で動作する
- [ ] 画像にファイル名を表示していない
- [ ] リンクが画像選択状態に応じて異なるページに遷移する

### 補助ページ

- [ ] 上部に画像が配置されている
- [ ] 下部に復帰リンクが配置されている
- [ ] 復帰リンクがBase64で難読化されている

### 公開後

- [ ] GitHub Pages上の実URLで表示確認する
- [ ] 画像が表示される
- [ ] フォントが適用される
- [ ] CSSが適用される
- [ ] JavaScriptが動作する
- [ ] 各ページの遷移が動作する
- [ ] 画像選択UIが動作する
- [ ] 再生UIが動作する
- [ ] 補助ページが動作する
- [ ] 404がない
- [ ] ブラウザコンソールに重大なエラーがない

---

# 8. 完成判定

本プロジェクトにおいて「完成」と判断できるのは、以下のすべてを満たした場合のみとする。

01. 仕様書のデザイン・実装規則を満たしている
02. ソースコード隠蔽規則を満たしている
03. ローカル環境で正常に動作する
04. GitHubリポジトリ上のファイル構成が正しい
05. GitHub Pages上の実際のURLで正常に動作する
06. 画像・フォント・CSS・JavaScript等のリソースがすべて正常に読み込まれる
07. 全ページの遷移が正常に動作する
08. 画像選択UIが正常に動作する
09. 再生UIが正常に動作する
10. 補助ページが正常に動作する
11. ファイル名の大文字・小文字の不一致がない
12. ローカルPC固有のパスやリソースに依存していない
13. GitHub Pages上で重大な404・リソース読み込みエラー・JavaScriptエラーがない

**ローカル環境で正常に動作しただけでは完成とはみなさない。**

GitHub Pagesに公開した実際の環境で正常に動作することを確認して初めて完成とする。

---

# 9. 引き継ぎ用プロンプト

他のAIに作業を引き継ぐ際は、以下のテキストを渡してください。

> **【指示文】**
> リポジトリ内の `HANDOVER.md` に記載されているARGWeb開発仕様を厳守してページの作成・更新を行ってください。
>
> 特に、親切設計の完全禁止、点字空白タイトル `U+2800` の維持、 **エントリURLは `index.html` で固定（乱数なし・永久不変）・中間ページは `main.html` で固定・補助ページは8桁ランダム英数字で更新のたび変更**、Base64難読化、**再生UI（音量50%・ページ遷移なし）+ 画像選択UI（左右矢印・ファイル名非表示）+ リンク（画像選択状態で遷移先分岐）の横並び配置**、**補助ページ（上部画像・下部復帰リンク）**、`x12y16pxMaruMonica.ttf` の全編Webフォント適用、レイアウト標準、**リンク色 `rgb(50, 117, 198)`・サイズ `3rem`**を厳守してください。
>
> また、本プロジェクトは **ローカル環境だけでなくGitHub Pages上で実際に動作することを完成条件とします**。
>
> HTML、CSS、JavaScript、画像、フォント等の参照には原則として相対パスを使用し、Windowsの絶対パス、`file:///`、不要なドメインルート相対パスを使用しないでください。
>
> ファイル名・フォルダ名・拡張子の大文字小文字を完全一致させ、GitHub PagesのLinux環境で404にならないようにしてください。
>
> ローカルでの動作確認だけを根拠に完成と判断せず、GitHub Pagesの実URLで画像、フォント、CSS、JavaScript、ページ遷移、画像選択UI、再生UI、補助ページ、404、コンソールエラーを確認してください。
>
> **「コードを書いた」「ローカルで動いた」「GitHubにpushした」だけでは作業完了とみなしません。公開URL上で仕様通り動作することまで確認してください。**
